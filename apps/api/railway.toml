[build]
builder = "nixpacks"
buildCommand = "pnpm install && pnpm --filter @aria/api build"

[deploy]
startCommand = "node apps/api/dist/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "aria-api"
